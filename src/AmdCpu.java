public class AmdCpu extends CPU {

    public AmdCpu(String brand, int numberOfCores, double clockSpeed, String gpuCompatibility) {
        super(brand, numberOfCores, clockSpeed, gpuCompatibility);
    }
}
